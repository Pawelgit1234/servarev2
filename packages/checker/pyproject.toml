[project]
name = "checker"
version = "0.1.0"
description = "Check ips found by scanner"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "common"
]

[project.scripts]
checker = "checker:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
