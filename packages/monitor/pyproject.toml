[project]
name = "monitor"
version = "0.1.0"
description = "Checks all found servers"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "common"
]

[project.scripts]
monitor = "monitor:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
