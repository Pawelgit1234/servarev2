[project]
name = "scanner"
version = "0.1.0"
description = "Scanns the internet for specific ports"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "common"
]

[project.scripts]
scanner = "scanner:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
